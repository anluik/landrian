export function Logo() {
    return (
        <span className="flex items-center gap-2.5 font-mono text-sm font-semibold tracking-widest uppercase">
            <span className="size-3 rotate-45 rounded-xs bg-primary shadow-[0_0_12px_color-mix(in_srgb,var(--color-primary)_60%,transparent)]" />
            Landrian
        </span>
    );
}
